import {View} from "react-native";
import ItemMenuTitle from "../../UI/ItemMenuTitle";

const MusculationForm = () => {
    return <View>
        <ItemMenuTitle children='musculation'/>
    </View>
}

export default MusculationForm;
